#!/usr/bin/env node

var str = 'THIS SHOULD NOT BE HERE'

var animate = require('./')(function (runtime) {
  return str
})

setTimeout(function () {
  str = 'ok'
  animate.stop(true)
},2000)

animate.start()